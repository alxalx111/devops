apiVersion: v1
kind: Service
metadata:
  labels:
    app: vote
  name: vote
spec:
  type: ClusterIP
  ports:
  - name: "vote-service"
    port: {{ .Values.services.vote.port }}
    targetPort: {{ .Values.services.vote.targetPort }}
  selector:
    app: vote